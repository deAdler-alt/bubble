# Builds / caches
node_modules
dist
.vite
*.tsbuildinfo
coverage
.cache

# Server (skopiujemy tylko to co potrzebne w Dockerfile, ignorujemy resztę)
server/node_modules
server/dist

# VCS / IDE
.git
.gitignore
.github
.vscode
.idea
.cursor

# Env (przekazujemy przez env_file w compose)
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example

# OS / logs
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker artifacts (NIE ignoruj `docker/` — tam jest nginx.conf używane w COPY)
Dockerfile
docker-compose.yml
docker-compose.*.yml
.dockerignore

# Docs (nie potrzebne w obrazie)
DOCKER.md
README.md
LICENSE
